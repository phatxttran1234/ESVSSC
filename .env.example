# VSSCS Environment Variables

# Database Configuration (Optional - uses local storage as fallback)
VITE_DATABASE_URL=your_neon_database_url_here

# Example:
# VITE_DATABASE_URL=postgresql://username:password@host/database?sslmode=require